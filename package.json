{
  "name": "qr-generator",
  "version": "1.1.0",
  "private": true,
  "dependencies": {
    "antd": "^3.12.1",
    "fontfaceobserver": "^2.1.0",
    "qr-code-with-logo": "^1.1.0",
    "qs": "^6.6.0",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-ga": "^2.5.6",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:docs": "rm -rf docs && npm run build && cp -r build docs",
    "release": "standard-version"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "husky": "^1.3.1",
    "standard-version": "^8.0.1"
  }
}
