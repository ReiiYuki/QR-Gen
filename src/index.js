import React from 'react';
import ReactDOM from 'react-dom';
import App from 'App';
import ReactGA from 'react-ga';

ReactGA.initialize('UA-133277763-1');

ReactDOM.render(<App />, document.getElementById('root'));
